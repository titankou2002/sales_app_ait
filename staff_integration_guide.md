# 👔 系統新增助理/司機人員設定指南 (Staff Integration Guide)

本指南說明如何在「高雅瓷物流與管理系統」中新增一位負責物流配送（如開車、加油、配送貨物）但**不分配客戶、不計入銷售業績 KPI** 的助理（司機）同仁。

系統已完成底層代碼升級：透過「金鑰管理後台」手動新增的同仁，會**自動加入物流指派名單與全知視角日誌**，且**不會**在銷售績效圖表上產生空白卡片。

---

## 🛠️ 新增同仁三步驟

### 第一步：在管理後台新增姓名並配發金鑰
1. 以管理員瀏覽器打開系統的 **「金鑰與白名單管理後台」**。
   * 網址格式為：`您的系統 Web App 網址?view=getkeys`
2. 切換至 **「內部同仁金鑰管理」** 分頁。
3. 在左側的「發行專屬庫存連結」面板中：
   * 在「同仁姓名」輸入新同仁的姓名（例如：`王小明`）。
   * 點選 **「發行金鑰並產生連結」** 按鈕。
4. 系統會自動為其生成專屬加密金鑰，並在右側列表中顯示：
   * **直達連結**：點選複製，並將此 URL 傳給新同仁。同仁使用此連結即可免登入直接進入「外勤系統」或「庫存查詢」。

---

### 第二步：綁定預設公務車車牌 (選填)
為了讓同仁在填寫「行駛日誌」與「保養紀錄」時能自動帶入車牌，請至 Google 試算表設定車牌綁定：
1. 打開 **主庫試算表 (Main DB)**。
2. 切換至 **「系統設定」** 工作表。
3. 新增一行資料：
   * **第 D 欄 (車號)**：輸入該同仁常開的車牌號碼（例如：`ABC-1234`）。
   * **第 E 欄 (車主/使用人)**：輸入同仁的姓名（必須與第一步的姓名一致，例如：`王小明`）。
4. 存檔後，同仁在手機端開啟「行駛日誌」時，系統即會自動填入該預設車號。

---

### 第三步：設定未下班提醒 Email (選填)
若希望系統在同仁忘記打下班卡時自動發送提醒信件，請在程式碼中配置其信箱：
1. 打開專案中的 `FieldWork.js` 檔案。
2. 找到最前方的 `FW_CONFIG.EMAILS` 區塊。
3. 新增一行，將同仁姓名與 Email 進行對應，例如：
   ```javascript
   EMAILS: {
     "高弘治": "titankou2002@gmail.com",
     "謝博皓": "sb780910@gmail.com",
     "陳勁多": "ghosts0125@gmail.com",
     "潘右森": "panus081231@gmail.com",
     "王小明": "xiaoming@example.com" // 👈 新增這一行
   }
   ```
4. 修改後，使用 `clasp push` 推送代碼即可生效。

---

## ⚙️ 系統底層運作機制與防禦設計

底層代碼已針對非業績業務角色（助理/司機）进行了相容性優化，具體邏輯如下：

| 模組功能 | 系統運作方式 | 設計優點 |
| :--- | :--- | :--- |
| **物流配送 (轉派單)** | 系統的 `getSalesList()` 已自動整合 `DIRECT_ACCESS_CUSTOM`。新同仁在管理後台新增後，**會自動出現在「派遣司機」及「轉派單據」的下拉選單中**。 | 無需修改代碼或手動新增到「業務分區」工作表，即可直接指派配送單給他。 |
| **外勤打卡 & 日誌** | 同仁使用其直達連結登入後，可正常執行「上下班打卡」、「填寫工作日誌」、「填寫里程油資」等所有外勤操作。 | 所有打卡和日誌均能自動回寫至 `智能_` 系列工作表。 |
| **全知視角 (每日報表)** | `getOmniscientLogs` 會自動讀取並合併 `DIRECT_ACCESS_CUSTOM` 名單。 | 管理員可在戰情室「全知視角」中，一併監看助理同仁的每日打卡時間、行駛里程與配送日誌。 |
| **業績 KPI 排除** | 業績模組 (`Sales.js`) 依然嚴格依賴 `SALES_CONFIG.SALES_PEOPLE` 名單。 | 助理同仁**不會**出現在業績總覽、年度業績柱狀圖或月度業績卡片中，避免無業績數據導致畫面出現 0 元空白卡片。 |

---
